# 📄 Паспорт проекта: YandexGPT Telegram Bot

> Автоматизированный ИИ-ассистент для создания карточек товаров на маркетплейсах  
> Работает 24/7 на VPS, с логированием, автоперезапуском и безопасным хранением секретов.

---

## 🧠 Возможности

- Принимает описание товара от пользователя в Telegram
- Генерирует SEO-оптимизированную карточку через YandexGPT
- Включает: анализ, название, описание, преимущества, характеристики, ключевые слова
- Работает без участия человека

---

## 🛠 Технологический стек

| Компонент             | Технология                               |
|-----------------------|------------------------------------------|
| Бэкенд                | Python 3.12                              |
| Telegram-фреймворк    | aiogram 3.x                              |
| ИИ-провайдер          | YandexGPT (yandexgpt-lite)               |
| База данных (логи)	  | PostgreSQL 17+                           |
| Библиотека для БД     |	psycopg2-binary                          |
| Система (Dev-хост)    | Ubuntu 25.04 LTS (LXC-контейнер)         |
| Управление процессами | systemd                                  |
| Логирование           | Файл /otp/ytb/src/postgres_logger.py     |
| Зависимости           | requirements.txt + виртуальное окружение |
| Безопасность          | Секреты в .env (не в Git!)               |

---

## 🚀 Деплой и запуск

### Требования

- Dev-хост: LXC-контейнер с Ubuntu 25.04 (проверено на домашнем сервере 192.168.1.247).
- База данных: Сервер PostgreSQL (например, на 192.168.1.249) с созданной базой ytb и таблицей bot_logs.
- API-ключ Yandex Cloud.
- Telegram Bot Token (из @BotFather).

### Установка (one-time setup)

```bash
# 1. Подключиться к контейнеру по SSH
# 2. Клонировать репозиторий
sudo git clone git@github.com:StenTar/yandexgpt-telegram-bot.git /opt/ytb
cd /opt/ytb

# 3. Создать виртуальное окружение и установить зависимости
python3 -m venv venv
venv/bin/pip install -r requirements.txt

# 4. Настроить переменные окружения (БД и API)
cp .env.example .env
nano .env  # Вставить BOT_TOKEN, YANDEX_API_KEY, FOLDER_ID, DB_HOST, DB_PASSWORD и др.
```

### Настройка systemd-службы на Dev-хосте

```bash
# Скопировать и активировать конфигурацию systemd внутри контейнера
sudo cp /opt/ytb/telegram-bot.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable telegram-bot
sudo systemctl start telegram-bot

# Проверить статус
sudo systemctl status telegram-bot
```

> ✅ После этого бот автоматически запускается при перезагрузке контейнера. Основные логи записываются в PostgreSQL, системные логи доступны через journalctl.
---

## 📊 Мониторинг и логи

# Архитектура логирования
Логи разделены на два уровня:

1. Бизнес-логика и события приложения: Хранятся в централизованной базе данных PostgreSQL (192.168.1.249) в таблице bot_logs. Включают уровень (INFO/ERROR), имя пользователя, ID, текст сообщения и ошибки.

3. Системные логи и вывод службы: Управляются через systemd и доступны via journalctl.

### Проверить статус службы

```bash
sudo systemctl status telegram-bot
```

### Просмотр логов

```bash
# 1. Системные логи службы (жизненный цикл, ошибки запуска)
sudo journalctl -u telegram-bot -f

# 2. Логи приложения в БД (основной способ)
psql -h 192.168.1.249 -U ytb_admin -d ytb -c "SELECT * FROM bot_logs ORDER BY timestamp DESC LIMIT 10;"
```

Пример запроса к таблице логов

```sql
<SELECT id, timestamp, level, username, message FROM bot_logs ORDER BY timestamp DESC LIMIT 5;>
```

Пример вывода:

```
 id |           timestamp           | level | username |                           message
----+-------------------------------+-------+----------+--------------------------------------------------------------
  5 | 2026-01-07 23:01:28.509267+06 | INFO  | tastemih | Карточка товара успешно создана, длина ответа: 1485 символов
  4 | 2026-01-07 23:01:23.782773+06 | INFO  | tastemih | Получено сообщение от пользователя: Газировка
  3 | 2026-01-07 23:00:22.760922+06 | INFO  | system   | 🚀 Бот запущен!
```

---

## 🔒 Безопасность

- Секреты НЕ в репозитории: .env добавлен в .gitignore
- API-ключи: хранятся только на сервере
- Обновление ключей: при компрометации — просто заменить в .env и перезапустить службу
- Сетевые права: служба работает от обычного пользователя (tastemih), без прав root

---

## 🔄 Обновление кода

```bash
# Перейти в директорию проекта
cd /opt/ytb

# Остановить службу
sudo systemctl stop telegram-bot

# Обновить код (например, через git pull)
git pull origin master

# Установить новые зависимости (если есть)
venv/bin/pip install -r requirements.txt

# Запустить службу заново
sudo systemctl start telegram-bot
```

---

## 📁 Структура проекта

```text
/opt/ytb/                                                 # Корень проекта на Dev-хосте
├── src/                                                  # Исходный код
│   ├── __init__.py
│   ├── bot.py                                            # Основной файл Telegram-бота
│   ├── yandexgpt_marketer.py                             # Модуль для работы с YandexGPT API
│   └── postgres_logger.py                                # Модуль логирования в PostgreSQL
├── venv/                                                 # Виртуальное окружение Python
├── .env                                                  # Файл с секретами (исключен в .gitignore)
├── .env.example                                          # Шаблон с настройками БД и API
├── requirements.txt                                      # Зависимости Python (вкл. psycopg2-binary)
├── telegram-bot.service                                  # Конфигурация systemd для контейнера
└── README.md                                             # Документация
```

💡 \*\*Проект организован в соответствии с практиками Python-разработки и DevOps:\*\* код изолирован в \`src/\`, используются виртуальное окружение и система инициализации.

💡 Этот проект — пример end-to-end delivery:  
от идеи и prompt-инжиниринга — до продакшен-деплоя с мониторингом и безопасностью.

---

## ✅ Статус готовности

- [x] Работает 24/7 на домашнем Dev-хосте (LXC-контейнер)
- [x] Автоматический запуск через systemd
- [x] Централизованное логирование в PostgreSQL
- [x] Обработка ошибок
- [x] Безопасное хранение секретов
- [x] Документация актуализирована

---

### 🏁 Готов к использованию в production!
