# 📄 Паспорт проекта: YandexGPT Telegram Bot

> Автоматизированный ИИ-ассистент для создания карточек товаров на маркетплейсах  
> Работает 24/7 на VPS, с логированием, автоперезапуском и безопасным хранением секретов.

---

## 🧠 Возможности

- Принимает описание товара от пользователя в Telegram
- Генерирует SEO-оптимизированную карточку через YandexGPT
- Включает: анализ, название, описание, преимущества, характеристики, ключевые слова
- Работает без участия человека

---

## 🛠 Технологический стек

| Компонент             | Технология                               |
|-----------------------|------------------------------------------|
| Бэкенд                | Python 3.12                              |
| Telegram-фреймворк    | aiogram 3.x                              |
| ИИ-провайдер          | YandexGPT (yandexgpt-lite)               |
| Система               | Ubuntu 24.04 LTS                         |
| Управление процессами | systemd                                  |
| Логирование           | Файл /otp/ytb/logs/telegram-bot.log     |
| Зависимости           | requirements.txt + виртуальное окружение |
| Безопасность          | Секреты в .env (не в Git!)               |

---

## 🚀 Деплой и запуск

### Требования

- VPS с Ubuntu 24.04 (проверено на Cloud.ru)
- Доступ по SSH
- API-ключ Yandex Cloud
- Telegram Bot Token (из @BotFather)

### Установка (one-time setup)

```bash
# Клонировать репозиторий в рекомендованную для проектов директорию /opt
sudo git clone https://github.com/StenTar/yandexgpt-telegram-bot.git /opt/ytb
cd /opt/ytb

# Создать виртуальное окружение
python3 -m venv venv
venv/bin/pip install -r requirements.txt

# Создать .env из шаблона и настроить секреты
cp .env.example .env
nano .env  # вставить свои ключи (BOT_TOKEN, YANDEX_API_KEY, FOLDER_ID)
```

### Запуск как служба

```bash
# Скопировать и активировать конфигурацию systemd
sudo cp /opt/ytb/telegram-bot.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable telegram-bot
sudo systemctl start telegram-bot

# Проверить статус
sudo systemctl status telegram-bot
```

> ✅ После этого бот автоматически запускается при перезагрузке сервера. Логи работы записываются в /opt/ytb/logs/bot.log.

---

## 📊 Мониторинг и логи

### Проверить статус

```bash
sudo systemctl status telegram-bot
```

### Смотреть логи в реальном времени

```bash
# Основные логи приложения (стандартный вывод)
tail -f /opt/ytb/logs/bot.log

# Логи ошибок (стандартный вывод ошибок)
tail -f /opt/ytb/logs/bot.error.log

# Или использовать системный журнал (может содержать больше служебной информации)
sudo journalctl -u telegram-bot -f
```

### Пример лога

```
🚀 Бот запущен!
📩 [stepan_tar (123456789)]: Bluetooth колонка с подсветкой
✅ Ответ для stepan_tar (123456789) готов (длина: 1245 символов)
```

---

## 🔒 Безопасность

- Секреты НЕ в репозитории: .env добавлен в .gitignore
- API-ключи: хранятся только на сервере
- Обновление ключей: при компрометации — просто заменить в .env и перезапустить службу
- Сетевые права: служба работает от обычного пользователя (tastemih), без прав root

---

## 🔄 Обновление кода

```bash
# Перейти в директорию проекта
cd /opt/ytb

# Остановить службу
sudo systemctl stop telegram-bot

# Обновить код (например, через git pull)
git pull origin master

# Установить новые зависимости (если есть)
venv/bin/pip install -r requirements.txt

# Запустить службу заново
sudo systemctl start telegram-bot
```

---

## 📁 Структура проекта

```text
/opt/ytb/ 						# Корень проекта
├── src/ 						# Исходный код (основной пакет)
│   ├── init.py
│   ├── bot.py 					# Основной файл Telegram-бота
│   └── yandexgpt_marketer.py 	# Модуль для работы с YandexGPT API
├── logs/ 						# Директория для логов
│   ├── bot.log 				# Стандартный вывод (stdout)
│   └── bot.error.log			# Вывод ошибок (stderr)
├── venv/ 						# Виртуальное окружение Python (исключено в .gitignore)
├── .env 						# Файл с секретами (исключен в .gitignore
├── .env.example 				# Шаблон для настройки секретов
├── .gitignore 					# Правила для Git
├── requirements.txt 			# Зависимости Python
├── telegram-bot.service     	# Конфигурационный файл для systemd
└── README.md 					# Документация проекта
```

💡 \*\*Проект организован в соответствии с практиками Python-разработки и DevOps:\*\* код изолирован в \`src/\`, конфигурация и логи вынесены отдельно, используются виртуальное окружение и система инициализации.

💡 Этот проект — пример end-to-end delivery:  
от идеи и prompt-инжиниринга — до продакшен-деплоя с мониторингом и безопасностью.

---

## ✅ Статус готовности

- [x] Работает 24/7 на VPS
- [x] Автоматический запуск после перезагрузки
- [x] Детальное логирование
- [x] Обработка ошибок
- [x] Безопасное хранение секретов
- [x] Документация

---

### 🏁 Готов к использованию в production!
