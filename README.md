# YandexGPT Telegram Bot: Production-Ready AI Assistant

Телеграм-бот для автоматического создания SEO-карточек товаров через YandexGPT API. Полностью развернутая система, работающая 24/7, демонстрирующая современные практики Python-разработки и DevOps.

---

## 🛠 Реализация и демонстрация навыков

Проект построен с акцентом на чистую архитектуру, безопасность и готовность к production. Каждый компонент демонстрирует конкретные компетенции разработчика:

*   **Python 3.12 & Асинхронное программирование (`aiogram 3.x`):** Бот использует паттерны `async/await` для неблокирующей обработки сообщений. Система роутеров (`aiogram.Router`) четко разделяет обработчики команд (`/start`) и текстовых сообщений, обеспечивая масштабируемость.
*   **Интеграция с внешним API (`requests`, YandexGPT API):** Класс `YandexGPTMarketerDetailedCoT` управляет полным циклом взаимодействия со сторонним AI-сервисом: установка заголовков авторизации (`Api-Key`), формирование точных JSON-запросов и парсинг структурированных ответов. Это демонстрирует навык работы со сторонними REST API.
*   **Prompt Engineering & работа с LLM:** Системный промт в `yandexgpt_marketer.py` — это сложный пример **Chain-of-Thought (CoT) промптинга**. Он направляет ИИ через двухэтапный процесс рассуждений (Анализ, Создание карточки), гарантируя структурированный и качественный вывод, адаптированный для e-commerce.
*   **Безопасность и управление конфигурацией (`python-dotenv`):** Чувствительные токены (токен бота, API-ключ Яндекс) хранятся в файле `.env` и загружаются при запуске через `load_dotenv()`. Это защищает секреты от попадания в систему контроля версий и соответствует методологии 12-factor app.
*   **Управление процессами в production (`systemd`):** Бот работает как управляемый системный сервис (`telegram-bot.service`). Это гарантирует автозапуск при загрузке, контроль над процессом и централизованное логирование — фундаментальный DevOps-навык для развертывания backend-приложений.
*   **Структурированное логирование и мониторинг:** Ключевые события (запросы пользователей, результаты генерации, ошибки) выводятся в `stdout`. Эти логи перехватываются `systemd` и направляются в `/opt/ytb/logs/`, что позволяет легко мониторить работу через `journalctl` или `tail -f`.
*   **Структура проекта и организация кода:** Код логически разделен на `src/bot.py` (логика Telegram) и `src/yandexgpt_marketer.py` (интеграция с ИИ). Такое разделение ответственности делает код поддерживаемым и тестируемым.

---

## 🚀 Развертывание (Production-настройка)

### Предварительные требования
* VPS с Ubuntu 24.04
* Python 3.12
* Токен Telegram-бота (от `@BotFather`)
* API-ключ Yandex Cloud и Folder ID

### Первоначальная настройка
```bash
# Клонирование и переход в директорию проекта
sudo git clone git@github.com:StenTar/yandexgpt-telegram-bot.git /opt/ytb
cd /opt/ytb

# Создание и активация виртуального окружения
python3 -m venv venv
venv/bin/pip install -r requirements.txt

# Настройка переменных окружения
cp .env.example .env
# Отредактируйте .env, вставив свои BOT_TOKEN, YANDEX_API_KEY, FOLDER_ID
```
### Настройка и активация службы
```bash
# Установка и включение службы systemd
sudo cp telegram-bot.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable --now telegram-bot

# Проверка статуса службы
sudo systemctl status telegram-bot
Результат: Бот теперь работает в фоне, автоматически перезапускается при сбое и стартует при загрузке системы.
```
### Мониторинг
```bash
# Просмотр логов приложения
tail -f /opt/ytb/logs/bot.log

# Просмотр логов ошибок
tail -f /opt/ytb/logs/bot.error.log

# Просмотр системных логов службы
sudo journalctl -u telegram-bot -f
```
## 📁 Структура проекта
```text
/opt/ytb/
├── src/                           # Пакет с исходным кодом
│   ├── __init__.py
│   ├── bot.py                     # Основная логика Telegram-бота (асинхронные обработчики, роутинг)
│   └── yandexgpt_marketer.py      # Клиент для работы с API YandexGPT (CoT-промпты)
├── logs/                          # Логи приложения (создаются автоматически)
│   ├── bot.log
│   └── bot.error.log
├── docs/                          # Документация проекта
│   └── 010-Project-Passport.md
├── .env.example                   # Шаблон для переменных окружения
├── requirements.txt               # Зависимости Python
├── telegram-bot.service           # Юнит-файл службы systemd
└── README.md                      # Этот файл
```
## 📄 Лицензия
MIT
